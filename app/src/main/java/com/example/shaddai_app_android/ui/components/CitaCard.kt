package com.example.shaddai_app_android.ui.components

import androidx.compose.runtime.*
import com.example.shaddai_app_android.model.CitaClima

@Composable
fun CitaCard(cita: CitaClima, onStatusChange: (Boolean) -> Unit) {
    // Aquí va el código del Card que hicimos antes...
    // El Checkbox llamará a onStatusChange(it)
}